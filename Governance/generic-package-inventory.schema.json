{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/thomasweidner/flashgate-mcp/Governance/generic-package-inventory.schema.json",
  "title": "FlashGate generic package inventory",
  "type": "object",
  "allOf": [
    { "if": { "properties": { "profile": { "const": "GENERIC_COMMIT_PREPARATION" } }, "required": ["profile"] }, "then": { "properties": { "transitionType": { "const": "COMMIT_PREPARATION_TO_COMMIT_APPROVAL" }, "entries": { "minItems": 9, "maxItems": 9 } } } },
    { "if": { "properties": { "profile": { "const": "IMPLEMENTATION_TO_INDEPENDENT_FULL_REVIEW" } }, "required": ["profile"] }, "then": { "properties": { "transitionType": { "const": "IMPLEMENTATION_TO_INDEPENDENT_FULL_REVIEW" }, "entries": { "minItems": 9, "maxItems": 9 } } } },
    { "if": { "properties": { "profile": { "const": "EVIDENCE_ONLY_FOCUSED_REVIEW" } }, "required": ["profile"] }, "then": { "properties": { "transitionType": { "const": "EVIDENCE_ONLY_TO_FOCUSED_REVIEW" }, "entries": { "minItems": 8, "maxItems": 8 } } } },
    { "if": { "properties": { "profile": { "const": "POST_MERGE_CLOSURE" } }, "required": ["profile"] }, "then": { "properties": { "transitionType": { "const": "POST_MERGE_TO_DOCUMENTATION_CLOSURE" }, "entries": { "minItems": 9, "maxItems": 9 } } } }
  ],
  "additionalProperties": false,
  "required": ["schemaVersion", "taskId", "profile", "transitionType", "entries"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "taskId": { "type": "string", "pattern": "^BL-(?:00[1-9]|0[1-9][0-9]|[1-9][0-9]{2,})$" },
    "profile": { "enum": ["GENERIC_COMMIT_PREPARATION", "IMPLEMENTATION_TO_INDEPENDENT_FULL_REVIEW", "EVIDENCE_ONLY_FOCUSED_REVIEW", "POST_MERGE_CLOSURE"] },
    "transitionType": { "enum": ["COMMIT_PREPARATION_TO_COMMIT_APPROVAL", "IMPLEMENTATION_TO_INDEPENDENT_FULL_REVIEW", "EVIDENCE_ONLY_TO_FOCUSED_REVIEW", "POST_MERGE_TO_DOCUMENTATION_CLOSURE"] },
    "entries": { "type": "array", "minItems": 8, "maxItems": 9, "items": { "type": "object", "additionalProperties": false, "required": ["path", "sha256", "length"], "properties": { "path": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$" }, "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }, "length": { "type": "integer", "minimum": 1 } } } }
  }
}
