// Package operation provides transport-neutral lifecycle primitives for
// bounded, long-running work. Business-domain validation and result semantics
// remain with the domain that owns the work.
package operation
